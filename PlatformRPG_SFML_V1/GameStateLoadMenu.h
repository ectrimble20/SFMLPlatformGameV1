#pragma once

// MainMenu -> LoadMenu, this state is used to display/load in a new saved game and will back itself out and launch into Running

class GameStateLoadMenu
{
public:
	GameStateLoadMenu();
	~GameStateLoadMenu();
};

